#include "main.h"
/**
 * *_idlower - function to check for lowercase character
 * @c: is the int that will use for the argument of the function
 * Return: 0
 */
int _inslower(intc)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);
}
